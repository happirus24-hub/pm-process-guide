# 작업일지 — 카카오페이 PM 프로세스 가이드

## 프로젝트 개요

- **레포지토리**: https://github.com/happirus24-hub/pm-process-guide
- **배포 URL**: https://happirus24-hub.github.io/pm-process-guide
- **주요 파일**:
  - `index.html` — 메인 서비스 출시 프로세스 가이드 페이지
  - `admin.html` — 링크 관리 어드민 페이지 (링크 추가/수정)
  - `links.json` — 각 태스크 카드에 연결되는 링크 데이터

---

## 작업 이력

### 2026-05-26

#### 추가: 단계별 진행 현황 프로그래스 바 (index.html)

**변경 위치**: `index.html` — 출시 프로세스 탭 > 요약 카드(summary-grid) 아래, 업무 목록(processContent) 위

**구현 내용**:
- 10개 프로세스 단계를 가로 스테퍼(stepper) 형태로 표시
- 각 단계 원형 버튼 클릭 → 완료(초록 ✓) / 미완료 토글
- 현재 진행 중인 단계(첫 번째 미완료 단계)를 카카오 노란색으로 강조 표시
- 완료된 단계 사이의 연결선이 초록색으로 변경
- 하단에 전체 진행률 바 (0~100%) 표시
- 완료 상태는 **localStorage**에 저장 → 페이지 새로고침 후에도 유지
- 초기화 버튼으로 전체 리셋 가능

**추가된 코드 구조**:

```
CSS:
  .phase-progress-wrap       — 전체 컨테이너 카드
  .phase-steps-row           — 스텝 아이템들을 가로로 나열하는 flex 컨테이너
  .phase-step-item           — 개별 단계 (원 + 라벨)
  .phase-step-item.completed — 완료 상태 스타일 (초록)
  .phase-step-item.current   — 현재 단계 스타일 (카카오 노란)
  .phase-step-circle         — 단계 번호 또는 ✓ 원형 버튼
  .phase-step-label          — 단계명 텍스트 (아이콘 + 이름)
  .step-connector            — 단계 사이의 가로 연결선
  .phase-overall-bar/fill    — 하단 전체 진행률 바

HTML (index.html ~line 580):
  <div class="phase-progress-wrap"> 안에 phaseSteps, phaseOverallFill 배치

JavaScript:
  phaseStates = {}            — 완료 상태 저장 객체 (key: phase.id, value: 'done')
  initPhaseProgress()         — localStorage에서 상태 불러온 후 renderPhaseProgress() 호출
  renderPhaseProgress()       — 스테퍼 HTML 생성, 진행률 업데이트
  togglePhaseComplete(id)     — 단계 완료 토글 + localStorage 저장
  resetPhaseProgress()        — 전체 초기화
```

**git 커밋**: `5b02b66` — "Add phase progress stepper to process guide"

---

#### 수정: 단계별 진행 순서 강제 (index.html)

**구현 내용**:
- 이전 단계가 완료된 경우에만 다음 단계 체크 가능
- 미완료 단계는 흐릿하게(opacity 40%) 표시, 클릭 무반응, `cursor: not-allowed`
- 완료 해제 시 이후 단계도 연쇄 해제 (예: 3단계 해제 → 4~10단계 자동 해제)

**추가된 CSS 클래스**:
- `.phase-step-item.locked` — 잠긴 단계 스타일 (흐림 + 클릭 불가)

**변경된 JS 함수**:
- `togglePhaseComplete()` — 이전 단계 완료 여부 검사 후 토글, 연쇄 해제 로직 추가
- `renderPhaseProgress()` — `locked` 클래스 및 tooltip 메시지 분기 추가

**git 커밋**: `45ab6b9` — "Enforce sequential phase completion in progress stepper"

---

#### 추가: 프로젝트 관리 기능 (index.html)

**변경 위치**: 헤더 바로 아래 `.project-bar` 추가, JS 전체 재구조화

**구현 내용**:
- 헤더 아래에 프로젝트 관리 바 신설
  - 드롭다운으로 프로젝트 전환
  - `✏️` 버튼으로 현재 프로젝트 이름 변경
  - `＋ 새 프로젝트` 버튼으로 신규 생성
  - `🗑️` 버튼으로 삭제 (마지막 프로젝트는 삭제 불가)
  - 이름 입력 시 Enter 확정 / Escape 취소
- 프로젝트별 독립적인 단계 완료 상태 저장
- 프로젝트 전환 시 해당 프로젝트의 진행 현황으로 즉시 업데이트
- 기존 `phaseProgressStates` 데이터를 "기본 프로젝트"로 자동 마이그레이션

**데이터 구조 (localStorage)**:
```
pmProjects (key) → JSON 배열
  [{ id: 'proj-xxx', name: '프로젝트명', phaseStates: { 'phase-common': 'done', ... } }, ...]

pmCurrentProjectId (key) → 현재 선택된 프로젝트 ID
```

**추가/변경된 JS 함수**:
```
projects[]            — 프로젝트 배열 (전역)
currentProjectId      — 현재 프로젝트 ID (전역)
loadProjects()        — localStorage에서 로드, 없으면 기본 프로젝트 생성
saveProjects()        — localStorage에 저장
getCurrentProject()   — 현재 프로젝트 객체 반환
renderProjectBar()    — 드롭다운 옵션 렌더링
switchProject(id)     — 프로젝트 전환
startProjectForm(mode)    — 이름 입력 폼 표시 ('create' | 'rename')
confirmProjectForm()  — 생성/변경 확정
cancelProjectForm()   — 폼 닫기
deleteCurrentProject() — 삭제 (confirm 팝업 후)
renderPhaseProgress() — getCurrentProject().phaseStates 기반으로 렌더
togglePhaseComplete() — 현재 프로젝트 상태 수정 후 saveProjects()
resetPhaseProgress()  — 현재 프로젝트 phaseStates 초기화
```

**git 커밋**: `0a5cd70` — "Add project management: per-project phase progress tracking"

---

### 2026-05-26 이전 작업 이력

| 커밋 | 내용 |
|------|------|
| `ab8ef78` | 링크 업데이트 (관리자) |
| `eac9a73` | 링크 관리 기능 추가: admin.html, links.json, 태스크 카드 링크 버튼 |
| `8e7f1d5` | PM 프로세스 가이드 웹페이지 최초 생성 |

---

## 현재 기능 목록

| 기능 | 위치 | 설명 |
|------|------|------|
| 출시 프로세스 탭 | index.html | 10개 단계별 업무 카드 목록, 검색/필터 |
| **프로젝트 관리 바** | index.html | 헤더 아래 — 프로젝트 생성/전환/이름변경/삭제 |
| **단계별 진행 현황** | index.html | 프로젝트별 프로그래스 바 + 순차 완료 스테퍼 |
| PM 체크리스트 탭 | index.html | 필수 체크항목, 완료 체크 및 진행률 |
| E2E 흐름도 탭 | index.html | 서비스 출시 단계별 흐름 설명 |
| 링크 관리 어드민 | admin.html | 태스크별 링크 등록/수정 (links.json 저장) |
| 검색 | index.html | 업무명, 설명, 도구, 조직 등 전문 검색 |
| 필터 | index.html | 전체 / 필수 업무만 필터 |
| 사이드바 단계 이동 | index.html | 클릭 시 해당 단계로 스크롤 |

---

## 다음 작업 시 참고사항

- **프로젝트 데이터**: localStorage `pmProjects` (배열), `pmCurrentProjectId` (문자열)
- **단계 완료 상태**: 각 프로젝트 객체 내 `phaseStates` 필드에 저장 (`{ 'phase-common': 'done', ... }`)
- 기존 `phaseProgressStates` 키는 더 이상 사용하지 않음 (첫 실행 시 마이그레이션 후 방치)
- **PM 체크리스트 완료 상태**: `checkStates` 객체 (localStorage 미저장 — 새로고침 시 초기화, 필요시 추가 가능)
- 링크 추가는 `admin.html`에서 관리, `links.json` 파일에 저장됨
- 배포는 GitHub Pages (main 브랜치 루트 디렉토리 기준)
- `node_modules/`, `package.json`, `package-lock.json` 은 xlsx 처리용 패키지로 `.gitignore`에 추가 검토 필요

---

## 잠재적 개선 아이디어 (우선순위 없음)

- [ ] 체크리스트 완료 상태도 localStorage에 저장 (새로고침 후 유지)
- [ ] 프로그래스 바에서 단계 클릭 시 해당 단계로 자동 스크롤
- [ ] 단계별 완료율 표시 (필수 업무 몇 개 완료했는지)
- [ ] 모바일 반응형 개선 (현재 900px 이하에서 사이드바 숨김)
- [ ] `node_modules` `.gitignore` 등록
