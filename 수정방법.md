# PM 프로세스 가이드 웹페이지 수정 방법

배포 URL: https://happirus24-hub.github.io/pm-process-guide

---

## 수정 절차

### 1. 파일 수정
`index.html` 파일을 텍스트 편집기(메모장, VS Code 등)로 열어 내용을 수정합니다.

### 2. 변경사항 GitHub에 반영
Claude Code 터미널에서 아래 명령어를 순서대로 실행합니다.

```
! git -C "C:\Users\hena.yang\.claude\hena_test" add index.html
! git -C "C:\Users\hena.yang\.claude\hena_test" commit -m "수정 내용 간단히 작성"
! git -C "C:\Users\hena.yang\.claude\hena_test" push
```

### 3. 배포 반영 확인
Push 후 1~2분 뒤 아래 URL에서 변경사항을 확인합니다.
https://happirus24-hub.github.io/pm-process-guide

---

## 자주 수정하는 항목

### 업무 항목 추가 / 수정
`index.html` 내 `const phases = [...]` 부분을 찾아 수정합니다.
각 업무 항목은 아래 구조로 되어 있습니다.

```js
{
  name: '업무명',           // 업무 카드 제목
  required: true,           // true = 필수, false = 선택
  desc: '업무 설명',        // 카드 클릭 시 보이는 상세 내용 (\n으로 줄바꿈)
  tools: ['도구1', '도구2'],// 사용 도구 목록
  orgs: ['팀명'],           // 검토 조직
  collabs: ['직군'],        // 협업 직군
  tfTask: '과제명',         // 프로덕션TF 과제 (없으면 '' 빈 문자열)
  note: '비고 내용'         // 비고 (없으면 '' 빈 문자열)
}
```

### 새 단계(Phase) 추가
`const phases = [...]` 배열 안에 아래 구조를 추가합니다.

```js
{
  id: 'phase-새아이디',     // 사이드바 이동용 고유 ID
  title: '단계명',
  icon: '🔧',              // 이모지
  color: '#색상코드',       // 제목 글자색
  bg: '#색상코드',          // 아이콘 배경색
  subPhases: [
    {
      name: '소단계명',
      tasks: [ /* 업무 항목들 */ ]
    }
  ]
}
```

### 체크리스트 항목 수정
`const checklistData = [...]` 부분을 찾아 수정합니다.

```js
{
  section: '섹션명',
  emoji: '📋',
  items: [
    '체크리스트 항목 1',
    '체크리스트 항목 2'
  ]
}
```

---

## 참고
- 로컬에서 미리보기: `index.html` 파일을 더블클릭하면 브라우저에서 바로 확인 가능
- GitHub 저장소: https://github.com/happirus24-hub/pm-process-guide
